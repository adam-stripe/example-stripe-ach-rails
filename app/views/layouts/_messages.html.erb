<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <ul class="list-unstyled">
      <% if value.is_a? String %>
        <li>
          <%= value %>
        </li>
      <% else %>
        <% value.each do |val| %>
          <li>
            <%= val %>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
<% end %>
